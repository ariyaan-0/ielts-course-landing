import CourseTitle from "../CourseTitle";

// import more as needed

const ContentCard = () => {
	return (
		<div className="p-6 rounded-lg shadow-md space-y-6">
			<CourseTitle />
			{/* Add CourseLayout, CoursePointers, etc. */}
		</div>
	);
};

export default ContentCard;
